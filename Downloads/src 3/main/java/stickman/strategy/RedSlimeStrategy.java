package stickman.strategy;

import stickman.Entities.Slime;
import stickman.levels.LevelImp;

public class RedSlimeStrategy implements SlimeStrategy{
    @Override
    public void move(Slime slime, LevelImp level) {
        /* Always keep static */
    }
}
